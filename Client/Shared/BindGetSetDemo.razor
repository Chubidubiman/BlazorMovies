<input type="text" @bind:get="text" @bind:set="SetAsync" @bind:event="oninput" />
<p>
    Text: @text
</p>

@code{
    private string text = string.Empty;
    private Task SetAsync(string val)
    {
        text = val.ToUpper();
        return Task.CompletedTask;
    }
}